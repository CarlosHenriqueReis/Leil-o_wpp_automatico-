# URL do webhook do N8N para INICIAR o leilão (URL Produção)
WEBHOOK_START_LEILAO=http://localhost:5678/webhook-test/start-leilao

# URL do webhook do N8N para ENVIAR COBRANÇA (URL Produção)
WEBHOOK_COBRANCA=http://localhost:5678/webhook/enviar-cobranca

# Chave secreta JWT (não altere em produção sem invalidar sessões existentes)
JWT_SECRET=super-key-pokemon-leilao-secret
